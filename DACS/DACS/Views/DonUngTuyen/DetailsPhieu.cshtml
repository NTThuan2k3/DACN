@model IEnumerable<DACS.Models.DonUngTuyen>

@{
    ViewData["Title"] = "DetailsPhieu";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<!-- Start banner Area -->
<section class="banner-area relative" id="home">
    <div class="overlay overlay-bg"></div>
    <div class="container">
        <div class="row search-page-top d-flex align-items-center justify-content-center">
            <div class="banner-content col-lg-12">
                <h1 class="text-white">
                    Tìm Kiếm Việc Làm
                </h1>
                <!--Search box-->
                @* @Html.Partial("../Shared/_SearchBoxPartial.cshtml")
                <p style="font-size: 20px" class="text-white"> <span>Search by tags:</span> Developer, Engineer, Technology, Support, Manager, Senior, Junior</p> *@
            </div>
        </div>
    </div>
</section>

<div class="container pt-100">
    <h1>CÁC ĐƠN ỨNG TUYỂN</h1>
    <table class="table">
        <thead>
            <tr>
                <th>
                    Họ Tên
                </th>
                <th>
                    CV
                </th>
                <th>
                    Mã đăng tuyên
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Users.FullName)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.TenCV)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.PhieuDangTuyenId)
                    </td>
                    <td>
                        @if (!string.IsNullOrEmpty(item.FileCV))
                        {
                            <a href="@Url.Content(item.FileCV)" class="btn btn-success" target="_blank">Tải CV</a>
                        }
                        else
                        {
                            <span class="text-danger">Chưa có CV</span>
                        }
                    </td>
                    <td>
                        <a class="btn btn-warning" asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

